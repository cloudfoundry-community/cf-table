require 'spec_helper'

describe Cf::Table do
  it 'has a version number' do
    expect(Cf::Table::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
